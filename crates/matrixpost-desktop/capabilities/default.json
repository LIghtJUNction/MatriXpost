{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window may use only Tauri core IPC permissions.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
